# Validation Rules

0 seeds  -> MISS (invalid)
1 seed   -> VALID
2 seeds  -> DOUBLE (invalid)
>2 seeds -> CLUSTER (invalid)

Current prototype uses per-frame classification.
Future version will implement drop-window event aggregation.
